<div class="row">
    <div class="col">
        <div class="card mb-3">
            <div class="card-body">
                <h4><b>Shopping Cart (<%= @cart.products.count %>) </b></h4>
            </div>
        </div>
        <% @cart.cart products.each do |cp| %>
            <div class="card mb-3">
                <div class="card-body">
                    <h4> <%= cp.product.name %> </h4>
                    <h6 class="text-muted"> <%= cp.product.category.name %> </h6>
                    <p> <%= cp.product.description %> </p>
                <div>
            <div>
        <% end %>
    </div>
    <div class="col">
        <!-- Información del precio total del carrito -->
        <div class="card">
            <div>
            </div>
        </div>
    </div>
</div>